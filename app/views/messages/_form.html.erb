<%= simple_form_for message, remote: true, html: { novalidate: false } do |f| %>
  <%= f.input :content, label: "#{user.username}: ", required: true %>
  <%= f.input :user_id, as: :hidden, input_html: { value: user.id } %>
  <%= f.button :submit, 'Send Message' %>
<% end %>
